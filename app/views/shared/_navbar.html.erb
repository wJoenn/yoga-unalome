<div class="navbar">
  <div class="logo">
    <%= vite_image_tag "images/logos/logo_text_white.png" %>
  </div>

  <div class="links">
    <nav>
      <a href="#">About</a>
      <span>✾</span>
      <a href="#">Sessions</a>
      <span>✾</span>
      <a href="#" id="contact-btn" data-action="click->contact#showForm">Contact</a>
      <span>✾</span>
      <div class="contact-button" data-contact-target="contactButton">
        <a href="#" id="mail-contact-btn" data-contact-target="icon" data-action="click->contact#showForm">
          <i class="fas fa-envelope"></i>
        </a>
      </div>
      <% if current_user %>
        <%= link_to "Account", dashboard_path %>
      <% else %>
        <%= link_to "Log in", new_user_session_path %>
      <% end %>
    </nav>
  </div>
</div>
