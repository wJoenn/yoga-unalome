<div class="navbar" data-controller="navbar-toggle">
  <%= vite_image_tag "images/logos/logo_text_white.png", class: "logo" %>

  <nav class="navbar-links" data-navbar-toggle-target="links">
    <a href="#">About</a>
    <span>✾</span>
    <a href="#">Sessions</a>
    <span>✾</span>
    <a href="#">Contact</a>
    <span>✾</span>

    <% if current_user %>
      <%= link_to "Account", dashboard_path %>
    <% else %>
      <%= link_to "Log in", new_user_session_path %>
    <% end %>
  </nav>

  <div class="toggle-button" data-action="click->navbar-toggle#toggleNavbar">
    <span class="dot"></span>
    <span class="dot"></span>
    <span class="dot"></span>
  </div>
</div>
