<div class="navbar">
  <div class="logo">
    <%= vite_image_tag "images/logos/logo_text_white.png" %>
  </div>

  <div class="links">
    <nav>
      <a href="#">About</a>
      <span>✾</span>
      <a href="#">Sessions</a>
      <span>✾</span>
      <a href="#">Contact</a>
      <span>✾</span>

      <% if current_user %>
        <%= link_to "Account", dashboard_path %>
      <% else %>
        <div data-controller="user-form-modal">
          <button data-action="click->user-form-modal#showForm">Log in</button>

          <%= render "shared/sign_in" %>
        </div>
      <% end %>
    </nav>
  </div>
</div>
